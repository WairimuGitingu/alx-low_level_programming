#include <stdio.h>

/**
 * main - initializes entry point
 * declaration : print all single digit numbers
 *
 * Result:0
 */

int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
	{
		printf(num);
	}
	printf("\n");
	return (0);
}
